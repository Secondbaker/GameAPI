<%= form_with(model: compatible_console, local: true) do |form| %>
  <% if compatible_console.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(compatible_console.errors.count, "error") %> prohibited this compatible_console from being saved:</h2>

      <ul>
        <% compatible_console.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
